.center {
    justify-content: center;
    align-items: center;
    height: 100vh;
}
.center > div {
    margin: 0;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

